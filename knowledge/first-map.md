# First Map

The first useful slice is not a semantic judge. It is a receipt machine for word usage.

For one term, the cartographer records dated occurrences, nearby words, examples, and a simple date-to-date neighbor shift score. That is enough to show when a term such as `agent` stops naming one thing and starts carrying several incompatible jobs.
